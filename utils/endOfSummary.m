function endOfSummary(level)
% The function should be called at the end of every summary method.
% Generates a new line if the summary is done (back to level 1).
if level == 1
    fprintf("\n");
end
end

